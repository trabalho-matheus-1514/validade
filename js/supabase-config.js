/**
 * Configuração do Supabase — Prazo Certo
 * -----------------------------------------------------------------
 * Preencha com os dados do SEU projeto Supabase:
 * Supabase Dashboard → Project Settings → API.
 *
 * SUPABASE_URL       → campo "Project URL"        (ex.: https://xxxxxxxx.supabase.co)
 * SUPABASE_ANON_KEY   → campo "anon public" em Project API keys
 *
 * A chave "anon" é feita para ficar no código do site (front-end);
 * quem protege os dados são as políticas de RLS definidas em
 * supabase/schema.sql — nunca cole aqui a chave "service_role".
 *
 * Se deixar os dois campos vazios, o app volta a funcionar apenas
 * com armazenamento local (localStorage), como antes — nada quebra.
 */
window.PRAZO_CERTO_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
